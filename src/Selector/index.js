import React from 'react';
import ButtonArea from './ButtonArea';

const Selector = () => (
    <div className="bg-warning p-3">
      <h2>This is a selector component</h2>
      <p>Select a language:</p>
      <ButtonArea />
    </div>
  );


export default Selector;